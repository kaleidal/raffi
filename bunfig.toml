[install]
# Use the root node_modules for the whole monorepo
linkWorkspacePackages = true
linker = "hoisted"

[install.scopes]
# If you ever publish scoped packages
"@raffi" = "https://registry.npmjs.org/"
